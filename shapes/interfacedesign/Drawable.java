package shapes.interfacedesign;

/**
 * Interface for drawable objects.
 * Classes implementing this interface should provide logic to visually represent themselves.
 */
public interface Drawable {
    /**
     * Draws the object (prints details to console or displays in a GUI).
     */
    void draw();
}
